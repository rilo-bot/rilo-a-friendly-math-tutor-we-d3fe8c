# rilo-a-friendly-math-tutor-we-d3fe8c

Built with **RILO**. This is a turborepo:

- `apps/web` — the frontend (Vite + React)
- `apps/api` — the backend (Express)

Local dev: `npm install && npm run dev`. Deployed on Render as two services (static web + Node API).
